name: Deploy to Windows Server
on:
  push:
    branches:
      - main  # Change to your deployment branch

jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        with:
          script: |
           ssh zenApp@64.23.197.171

            
